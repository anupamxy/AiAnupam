# exception handling is a mechanism to handle errors gracefully in Python. It allows you to catch and handle exceptions (errors) that may occur during the execution of your program, preventing it from crashing and providing a way to respond to the error.
# index error
# key error
# zero division error
# type error
